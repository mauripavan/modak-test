import React from 'react';
import {Text} from 'react-native';

import {StyledView} from './styles';

const HomeScreen = () => {
  return (
    <StyledView>
      <Text>React Navigation initial config</Text>
    </StyledView>
  );
};

export default HomeScreen;
